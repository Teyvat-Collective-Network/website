##########
# PUBLIC #
##########

# export configuration
PUBLIC_DOMAIN="the domain of this website"
PUBLIC_COOKIE_DOMAIN="the domain for cookies (should be the same as PUBLIC_DOMAIN unless it is localhost, in which case leave this empty)"

# import configuration
PUBLIC_API="the URL of the public API"
PUBLIC_WEBSOCKET="the URL of the public API websocket"

# secrets configuration

# development configuration

# other values
PUBLIC_DISCORD_ID="the application ID of the primary bot"
PUBLIC_HQ="the ID of the TCN HQ server"
PUBLIC_HUB="the ID of the TCN Hub server"
PUBLIC_WEEKLY_SUMMARY_PING_ROLE="the ID of the weekly summary ping role in HQ"
PUBLIC_WEEKLY_SUMMARY_OBSERVER_PING_ROLE="the ID of the weekly summary observer ping role in HQ"

###########
# PRIVATE #
###########

# export configuration

# import configuration
BOT_API="the URL of the bot's controller API"
PRIVATE_API="the URL of the private API"

# secrets configuration
DISCORD_SECRET="the Discord secret for OAuth of the primary bot"
WEBHOOK_SUMMARIES="the webhook URL for the summaries channel"
WEBHOOK_OBSERVER_GALLERY="the webhook URL for the read-only observer channel"

# development configuration

# other values